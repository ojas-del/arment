---
name: Clone site audit - page inventory and structure
description: Complete inventory of pages, sections, and route status for the Pure Pet Food clone site at localhost:3001 as of 2026-03-19
type: project
---

Clone site at http://localhost:3001/ is a Next.js app cloning purepetfood.com.

**Working pages (HTTP 200):**
- `/` (homepage) - full landing page with 12+ sections
- `/about` - company history timeline (2012-2024)
- `/recipes` - dog food recipes with recipe cards and filtering (general/sensitive/renal)
- `/reviews` - customer reviews, vet endorsements, categorized testimonials
- `/signup` - onboarding flow starting with dog name input
- `/benefits` - health benefits page (linked as "Health & breeds" in nav)

**404 pages:**
- `/how-it-works`, `/why-pure`, `/help`, `/login`, `/blog`, `/breeds`, `/puppies`, `/pet-panel`, `/dog-professionals`, `/careers`, `/research`, `/contact`, `/account`, `/delivery`, `/privacy`, `/terms`, `/returns`, `/security`, `/sitemap`, `/beyond-the-bowl`, `/policies`

**Why:** Baseline inventory for tracking progress on the clone build.
**How to apply:** Use this to know which pages need building and which already exist when planning work.
